<% flash.each do |message_type, message| %>
<font color="#50a649">
  <%= content_tag :div, message, class: message_type %>
</font>
<% end %>
<h1><%= @title %></h1>
   <table class="table">
        <tr>
            <th>グループ名</th>
        </tr>
         <tr>
            <td><%= @teamShow.teamname%></td>
         </tr>
    </table>