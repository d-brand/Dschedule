<h1> スケジュール調整</h1>
  <div style="clear: both; padding: 10px 0 20px;"><%= link_to "スケジュールの確認画面へ", new_user_session_path, {class: "btn-border" }%></div> <!-- ログイン画面に移行する -->
  <% if !user_signed_in? %>
  <!-- 新規登録画面に移行する -->
    <div style="clear: both; padding: 10px 0 20px;"><%= link_to "新規登録", new_user_registration_path, {class: "btn-border" }%></div>
  <% end %>
